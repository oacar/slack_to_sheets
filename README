This project is a web app that copies messages from a slack channel to google sheets document.

The goal was to create a Google Sheets document that backups the links shared. 

It uses both Google's and Slack's APIs.
